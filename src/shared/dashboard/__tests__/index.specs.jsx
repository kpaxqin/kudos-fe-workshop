import { mount } from 'enzyme'
import React from 'react'
import { DashboardComponent } from '../index'
import { MemoryRouter } from 'react-router-dom'

/* TODO: To be fixed, currently its failed becuz of context issue */
// describe('Dashboard Component', () => {
  // it('should add classes to document body when component will mount', () => {
  //   mount(
  //     <MemoryRouter>
  //       <DashboardComponent signOutAction={() => {}}><i>1</i></DashboardComponent>
  //     </MemoryRouter>
  //   );
  //   expect(document.body.className).to.equal('hold-transition skin-blue fixed sidebar-mini');
  // });
  //
  // it('should remove classes from document body when component will unmount', () => {
  //   const wrapper = mount(
  //     <MemoryRouter>
  //       <DashboardComponent signOutAction={() => {}}><i>1</i></DashboardComponent>
  //     </MemoryRouter>
  //   );
  //   wrapper.unmount();
  //   expect(document.body.className).to.be.empty;
  // });

// });

